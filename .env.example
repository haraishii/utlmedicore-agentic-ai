# MongoDB Configuration
MONGO_URL=mongodb://username:password@host:port/
MONGO_DB_1=DCA632971FC3
MONGO_DB_2=2CCF6754457F
MONGO_COLLECTION=posture_data

# Flask Configuration
FLASK_SECRET_KEY=your-secret-key-here
FLASK_PORT=5000

# Opik API Configuration (for model evaluation)
OPIK_API_KEY=your_opik_api_key_here
OPIK_WORKSPACE=default

# AI Model Configuration (Optional Override)
# DEFAULT_AI_MODEL=ollama:lfm2.5-thinking:1.2b

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. NEVER commit .env file to GitHub
# 4. .opik.config file is also git-ignored for security
